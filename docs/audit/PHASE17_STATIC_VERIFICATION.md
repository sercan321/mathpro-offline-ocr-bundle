# MathPro Flutter Phase 17 — Static Verification

- Full ZIP created, not a patch package.
- Package identity set to `mathpro_flutter_phase17` / `0.17.0+17`.
- Known stale `deriveSlotRects(size:)` regression pattern checked.
- Known stale `Phase 3 evaluator` user-facing text checked.
- Dirty archive entries excluded: `build/`, `.dart_tool/`, `.gradle/`, `__pycache__/`, `.pyc`, `.pytest_cache/`.
- Keyboard source files intentionally preserved.

Flutter analyze/test/run must be executed on the user's local Flutter SDK.
