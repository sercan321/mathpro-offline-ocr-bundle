# MathPro V67 Issue-to-File Matrix

Created: 2026-05-14 20:45:10 UTC

Scope: audit-only snapshot before modifying production code.

| # | Issue | User-visible failure | Primary files | Repair constraint |
|---:|---|---|---|---|
| 1 | Shared outer width | Workspace, Solution Steps and Keyboard must visually share the Solution Steps panel outer width. | `lib/app/app_shell.dart`, `lib/features/workspace/workspace_panel.dart`, `lib/features/keyboard/bottom_dock.dart` | Do not edit `solution_steps_panel.dart`. Fix via parent width constraints / shared shell metrics. |
| 2 | Long expression navigation | Long numbers, long equations and nested radicals/functions cannot be inspected horizontally. | `lib/features/workspace/editor_viewport.dart`, `editor_interaction_layer.dart`, `viewport_coordinate_transform.dart`, `render_mapped_slot_hit_test.dart` | InteractiveViewer currently pans only for `length > 64 && !contains(□)`, which blocks nested template navigation. |
| 3 | Nested cursor precision | Cursor movement is difficult inside `ln(ln(...))`, roots, fractions and template chains. | `editor_viewport.dart`, `editor_interaction_layer.dart`, `calculator_controller.dart`, `selection_controller.dart` | Must preserve existing slot overlay / caret tests while adding reliable long-expression movement. |
| 4 | All visible boxes tappable | Every visible `□` in keyboard/MORE/long-press/workspace needs a large invisible hit target. | `math_layout_engine.dart`, `slot_hit_testing.dart`, `render_mapped_slot_hit_test.dart`, `math_label.dart`, `long_press_popup.dart`, `premium_key.dart` | Hit targets already have test coverage; future repair must include transformed coordinate tests. |
| 5 | Atomic deletion | Functions/templates inserted from keyboard/MORE/long-press must not delete into `ln(`, `ln`, `l`, `si`, etc. | `calculator_controller.dart`, `editor_commands.dart`, `action_utils.dart`, `key_handlers.dart` | Requires structural token/template boundary detection, not raw character-only backspace. |
| 6 | Subscript optical distance | `x₁`, `x₂`, `x□` subscript is too far from base. | `math_label.dart`, `premium_key.dart`, `long_press_popup.dart` | Render-only repair; do not change x long-press option order unless explicitly required. |
| 7 | Derivative order notation | `y⁽ⁿ⁾`/`y^(n)` should not remain awkward; numeric order such as 4 should become premium derivative notation. | `key_config.dart`, `math_label.dart`, `math_tex_serializer.dart`, `action_utils.dart`, `calculator_controller.dart` | Needs display semantics and possibly transformation rule; no solver work. |
| 8 | Global math visual normalization | Keyboard, MORE and long-press symbols must use consistent mathematical layout. | `math_label.dart`, `premium_key.dart`, `long_press_popup.dart`, `action_utils.dart` | Render normalization phase only; no key re-layout. |
| 9 | Log templates | Duplicate/awkward `log(□)`-style entries and `log_1010`-looking rendering must be corrected. | `key_config.dart`, `action_utils.dart`, `math_label.dart`, `math_tex_serializer.dart` | General custom base target: `log` with close lower base slot plus body slot. |
| 10 | Percent insertion | `%` currently appears as blank/space in the editor. | `key_config.dart`, `key_handlers.dart`, `calculator_controller.dart`, `math_render_surface.dart`, `math_tex_serializer.dart` | Likely render/serializer issue, not keyboard layout. |
| 11 | Operator replacement | Consecutive + − × ÷ should replace the previous operator instead of accumulating. | `calculator_controller.dart`, `calculator_actions.dart`, `key_handlers.dart` | Must preserve unary minus / negative number behavior. |
| 12 | Fraction long-press removal | Fraction key normal tap stays; fraction long-press options should be disabled/removed. | `key_config.dart`, `bottom_dock.dart`, tests | Explicit user request; this is allowed despite prior long-press freeze. |
| 13 | Inverse trig rendering | `asin/acosh/atanh` style labels should display as `sin⁻¹`, `sinh⁻¹` etc. | `key_config.dart`, `action_utils.dart`, `math_label.dart`, `math_tex_serializer.dart` | Must preserve underlying evaluator names where needed; display can differ from inserted semantic text. |
| 14 | Root layout | Root sign and body box are too far apart; long-press roots must match premium math layout; n-root should use a degree box. | `key_config.dart`, `action_utils.dart`, `math_label.dart`, `premium_key.dart`, `long_press_popup.dart` | Existing template `□√(□)` already exists; render optics are weak. |
| 15 | Factorial long-press cleanup | Only `□C□` and `□P□` should remain under factorial; remove Gamma/Kalan there. | `key_config.dart`, `action_utils.dart`, tests | Explicit user request; do not remove MORE category items unless phase scope says so. |
| 16 | Absolute value long-press removal | Absolute value normal tap stays; all absolute-value long-press options removed. | `key_config.dart`, tests | Explicit user request. |
| 17 | Popup/key/workspace visual consistency | Long-press, MORE, visible keyboard and inserted workspace result must share one premium math language. | `math_label.dart`, `premium_key.dart`, `long_press_popup.dart`, `math_render_surface.dart` | Final visual cohesion check after phases 3-6. |
