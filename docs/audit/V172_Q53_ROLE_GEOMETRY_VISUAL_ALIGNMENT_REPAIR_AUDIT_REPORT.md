# V172-Q53 Integral Root Fraction Function Role Geometry Repair Closure

Q53 is a narrow, P6-locked cursor/editor geometry phase. It preserves Q51 placeholder restoration and Q52 active empty-slot `|□` behavior, then adds role-specific geometry polish for the real-device defect families shown in screenshots: fraction numerator/denominator, function arguments, radical/radicand/root index, integral upper/lower/body/differential, log base, powers and subscripts.

## Scope

- Added `RoleGeometryVisualAlignmentPolicy` as the Q53 authority for photographed role families.
- Wired Q53 through deterministic slot geometry, render-mapped slot hit testing, native LayoutBox caret geometry, active empty-slot leading rail finalization, and structural cursor hardening.
- Preserved Q52 rule: empty active slot caret stays immediately before the placeholder as `|□`; it is not moved into the placeholder box.
- Kept filled slot caret movement separate from empty slot leading-rail behavior.

## Protected surfaces

Keyboard order, key faces, MORE tray, long-press popup, app shell, MathLabel, template tray, Solution, Graph, and History remain protected. Q53 does not intentionally edit those surfaces.

## Honest limits

Q53 does not claim final Photomath/Wolfram +95 quality, full native math rendering, stable AST slot to SVG-node identity, or real-device completion. Device evidence must still come from user-side `flutter analyze`, `flutter test`, and `flutter run -d 23106RN0DA` plus screenshots/video.

## Expected package identity

```yaml
version: 0.172.85+172
description: MathPro Flutter phase 17 - V172-Q53R2 real Flutter log and stale-version guard repair.
```
