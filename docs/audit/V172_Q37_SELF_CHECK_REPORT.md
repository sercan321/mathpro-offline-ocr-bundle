# V172-Q37 Self Check Report

## Result

Static self-check completed with P6 locked files preserved: **PASS**.

## Locked File Evidence

- `lib/features/keyboard/key_config.dart`: **SAME**
- `lib/features/keyboard/math_keyboard.dart`: **SAME**
- `lib/features/keyboard/bottom_dock.dart`: **SAME**
- `lib/features/keyboard/long_press_popup.dart`: **SAME**
- `lib/features/keyboard/premium_key.dart`: **SAME**
- `lib/features/workspace/math_label.dart`: **SAME**
- `lib/features/workspace/template_tray.dart`: **SAME**
- `lib/app/app_shell.dart`: **SAME**
- `lib/features/solution/solution_steps_panel.dart`: **SAME**
- `lib/features/graph/graph_card.dart`: **SAME**
- `lib/features/history/history_controller.dart`: **SAME**
- `lib/features/history/history_panel.dart`: **SAME**

## Required Real Flutter Evidence

Flutter/Dart are not available in this assistant container. Do not claim `flutter analyze`, `flutter test`, or physical-device PASS until the user runs them.

Required commands:

```powershell
flutter pub get
flutter analyze
flutter test
flutter run -d 23106RN0DA
```

## Required Device Fraction QA

- `9/6` numerator tap and denominator tap.
- `12/345` payda internal caret target.
- `123/4567` repeated pay/payda tap attempts.

Expected: at least 19 correct selections per 20 attempts for the core fraction lane test before +95 claims.
